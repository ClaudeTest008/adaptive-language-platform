name: adaptive_language_platform
description: Adaptive Language Platform — an AI language teacher that learns how you learn.
publish_to: "none"
version: 0.2.0+2

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.0.0
  go_router: ^16.0.0
  flutter_tts: ^4.2.0
  speech_to_text: ^7.0.0
  shared_preferences: ^2.3.0
  crypto: ^3.0.0
  sherpa_onnx: ^1.13.4
  audioplayers: ^6.8.1
  archive: ^4.0.9
  path_provider: ^2.1.6
  # Phase 36: real on-device GGUF inference (llama.cpp via llamadart).
  llamadart: ^0.8.16
  llamadart_llama_cpp_flutter: ^0.0.10
  # Phase 37: raw PCM microphone capture for offline Whisper STT.
  record: ^7.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/curriculum/
    - assets/stories/
