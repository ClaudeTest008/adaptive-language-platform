# AI Context

This file is the permanent memory of the project for AI development sessions. Read it at the start of every session. Keep it accurate and short.

## What This Project Is

**Adaptive Language Platform** — AI-powered adaptive language learning system:

- Flutter client app (`app/flutter/`)
- Backend services (`backend/`)
- Serverless functions (`cloud_functions/`)
- Adaptive Learning Core (inherited, `lib/adaptive/` + `lib/ai/`) — see Lineage below

Goal: a personalized AI language teacher, not a flashcard app. The adaptive engine tracks mastery of language **concepts** (grammar patterns, vocabulary, skills), not individual exercises.

## Lineage (ADR-0014)

Forked 2026-07-12 from `adaptive-exam-platform` with full git history. Everything up to commit `3b597b2` is inherited exam-platform work. The Adaptive Learning Core is reused, not rewritten:

- `lib/adaptive/` — learner model, knowledge graph, scheduler, selector, confidence, Learning DNA (pure Dart)
- `lib/ai/` — AiChatModel provider seam, AiOrchestrator
- `lib/application/` — import pipeline, quality engine, document ingestion (becomes language content intelligence)
- `lib/domain/` + `lib/infrastructure/` — Clean Architecture repository pattern, demo mode (ADR-0006)
- Content Studio, multi-tenancy, search/notification seams

Layering: **Adaptive Learning Core → Adaptive Language Platform → Language Learning Features.** Extend the core with language signals; never rewrite it. Keep language-specific features separate from the core.

## Rules for AI Sessions

1. Read `PROJECT_STATUS.md` and `TASKS.md` before starting work.
2. Complete one well-defined task at a time; do not start unrelated work.
3. Read only the documentation referenced by the current task.
4. Never recreate files that already exist; continue from the current state.
5. Record architectural decisions in `docs/decisions/` (one file per decision).
6. Update `PROJECT_STATUS.md`, `TASKS.md`, and `CHANGELOG.md` when a milestone or task completes.
7. Keep commits small and focused; report the commit hash after each commit.
8. Never push to or modify the original `adaptive-exam-platform` repository.

## Language Learning Model

Knowledge hierarchy (replaces exam category → topic → question):

Language → Level → Skill → Domain → Topic → Grammar Concept → Vocabulary Concept → Phrase → Example Sentence → Exercise → Conversation

Skills with independent mastery: Vocabulary, Grammar, Reading, Writing, Listening, Speaking, Pronunciation, Conversation, Culture, Comprehension.

Language-specific memory signals (extend LearnerModel, don't replace): vocabulary strength, grammar confidence, recall speed, pronunciation confidence, listening recognition, speaking ability, common mistakes, native-language interference, retention decay.

Misconceptions tracked separately from mistakes (e.g. "Yo soy cansado" = English grammar transfer → teach `tener` pattern family).

AI tutor modes (architecture in Phase 3): Teacher, Conversation, Coach, Socratic, Grammar, Immersion. Tutor consumes learner history, knowledge graph, Learning DNA, mistakes, weak concepts, goals, learning style. Provider-independent (OpenAI/Anthropic/Gemini/local/speech/translation); all AI output passes validation.

## Current State

- Phase 29 (2026-07-18, no ADR, branch feature/phase16-dashboard-redesign): **Speaking analytics upgrade + P28 review + Whisper robustness. REAL WHISPER INFERENCE NOT DELIVERED (blocked: no PCM-capture plugin + no device).** P28 cache reviewed = correct; fixed metric side-effect: `PiperAudioCache.has(key)` pure probe added (prefetch uses it instead of `contains` so background probes don't skew hitRate). `speaking_session.dart` `analyzeSpeaking(target,transcript,{durationMs,retries,responseLatencyMs,conceptId})` now +`SpeakingSession{selfCorrections(_correctionMarkers regex 'no digo|quiero decir|mejor dicho|perdón|es decir|i mean|sorry'),restarts(adjacent dup words),speechRateWpm(wps·60, null w/o duration),responseLatencyMs}`; confidence penalty +selfCorrections·0.05; all measured-only null-when-absent. `whisper_model_manager.dart` +`repair({onState})` (verify→fail→delete+ensureDownloaded) +`storageBytes()`. Voice-pipeline guarantee regression-tested (speechSafeText English-never-spoken, immersion hides support/mentor keeps text). 370 tests (+9 `test/language_phase29_test.dart`). analyze clean, build green. **CRITICAL HONESTY: Phase goal (genuine local Whisper) NOT achieved — needs raw-PCM mic plugin (not a dep) + physical Android; fallback platform recognizer still the STT path; sherpa OfflineRecognizer isolate + PCM capture remain documented seams. NOT device-verified; no recognition/latency numbers claimed. Delivered only verifiable offline parts.** Next = device session for P28+P29 native verification, OR P30 GGUF (also device-blocked) — recommend pausing native phases until hardware available; pure-engine phases (P31 roleplay/outcomes, P34 lesson memory, P35 recommendations) remain fully deliverable.

- Phase 28 (2026-07-18, no ADR, branch feature/phase16-dashboard-redesign): **Real Piper audio cache + prefetch — NOT DEVICE-VERIFIED.** `audio_cache.dart`: `audioCacheKey` now returns `tts_<langslug>_<voiceslug>_<hex>.wav` (readable lang+voice prefix for scoped invalidation); +`audioCacheVoicePrefix({langCode,voice})`. `infrastructure/piper_audio_cache.dart` `PiperAudioCache(dir)` (dart:io): `pathFor/contains(hit/miss counters)/touch(in-mem tick)/store(key,srcPath — copy+delete tmp, no-op if src==dest)/cleanup({maxBytes,keep}) LRU via evictionPlan (never deletes keep)/invalidateVoice({langCode,voice}) prefix-delete/clear/sizeBytes; hits/misses/hitRate = SYSTEM metrics NOT learner data`. `PiperSpeechService` wired SURGICALLY (isolate/gen/barge-in/ANR path UNCHANGED): `_run` per-sentence now `audioCacheKey(text=s,lang,voice=_voices[base].dir,speed)` → cache hit plays cached path (no synth) / miss `_synthesize`→tmp→`_cache.store`→play; `_playFile(path,gen,{deleteAfter=true})` — cache plays deleteAfter:false (cache owns files); `_cacheMaxBytes=200MB`; `_playingKey` guards eviction; `cacheHitRate`/`cacheSizeBytes()` getters. `prefetch(texts,{langCode,speed})` synth-ahead-to-cache, aborts when `_gen!=startGen` (playback/stop) → never delays Play; `invalidateVoice(langCode)`. Reader: `_prefetchAround(story,i)` prefetches page i + i+1 (Piper-only via `is PiperSpeechService`, guarded `_prefetched` first-load + onPageChanged); imports piper_speech_service. 361 tests (+8 `test/language_audio_cache_test.dart` real-tempdir cache tests). analyze clean, build green. **CRITICAL HONESTY: NO physical device in env → device verification (latency first-vs-cached, chapter/voice switch, prefetch, large book, cache cleanup, cold/warm start, no-ANR rapid play/stop) NOT PERFORMED; no latency numbers claimed. Touches the ANR-critical Piper file — must be hardware-verified before trusted. Committed anyway (per never-leave-work-local) with loud disclosure; revert if device testing fails.** Next = P29 real Whisper inference OR device-verify P28.

- Phase 27 (2026-07-18, no ADR, branch feature/phase16-dashboard-redesign): **PDF/EPUB backend + Learning Workspace.** `lib/language/book_ingestion.dart` (PURE, no learner data): `ingestBook({title,author,text})→IngestedBook{chapters:List<IngestedChapter{title,paragraphs,sentences}>,wordFrequency,phrases,difficulty,estimatedCefr,topics,language,wordCount,chapterCount}`; helpers `splitChapters`(regex `^(chapter|capítulo)\s+…` multiline; no headings=1 chapter), `segmentSentences`, `detectLanguage`(es/en function-word sets), `inferTopics`(_topicLexicon travel/food/family/nature/work/technology), `estimateDifficulty(sentences)→(difficulty,cefr)` (avgWordLen 4→7 + avgSentLen 8→22 → 0-1 → a1..c1), `extractPhrases`(bigrams count≥2). `infrastructure/document_parser.dart`: `DocumentImporter.import({title,bytes})→ImportOutcome{ok,format:BookFormat{txt,epub,pdf,unknown},book?,message?}`; `detectFormat`(magic: %PDF→pdf, PK→epub, else txt); TXT+EPUB REAL offline (EPUB=ZipDecoder→_spineOrder from OPF manifest+spine→_stripHtml XHTML→text; _opfMetadata dc:title/creator; fallback sorted html files); **PDF→polite failure msg (NO OCR, no parse), scanned unsupported**; never crashes. `lib/language/book_analytics.dart` (PURE, measured-only): `computeReadingAnalytics({pagesReached,totalPages,chaptersRead,wordsRead,durationMs?,pagesRevisited?,wordTaps?,sentenceReplays?})→ReadingAnalytics{completionPercent,chaptersRead,readingSpeedWpm?/reReadRate?/tapFrequency?/timeSpentMs?/sentenceReplays? NULL unless measured}`; `VocabularyEntry{word,firstSeenDay,lastSeenDay,timesEncountered,timesLookedUp,sourceBookId?,chapter?,contextSentences,confidence?(NULL until real signal)}`+json+`recordEncounter(existing?,…)` merge; `BookFingerprint.fromIngested(id,book)` + `relateBooks(List)→List<BookRelationship{sharedTopics,sharedVocabCount,strength(topic·0.2+vocab-Jaccard)}>` (book-to-book, NOT concept graph). `lib/language/audio_cache.dart` (PURE): `audioCacheKey({text,langCode,voice,speed})` FNV-1a→`tts_<lang>_<hex>.wav` stable; `AudioCacheEntry` + `evictionPlan(entries,maxBytes)` LRU (Piper wiring = P28). experience.dart +`storyFromIngested({id,book})` (chapters→pages + chapterTitles/Starts if >1 chapter, level=estimatedCefr, topics, author). providers: importedBooksProvider now ingests→storyFromIngested; +`bookRelationshipsProvider`. importPlainText kept (unused now but public). 353 tests (+19 `test/language_book_test.dart` incl. crafted-EPUB via ZipEncoder). analyze clean, build green. **HONEST SEAMS: PDF text extraction NOT implemented (needs pure-Dart PDF backend — declared, polite-reported); file-picker byte import NOT wired (UI still paste-text→TXT; DocumentImporter ready for bytes); reading-analytics/vocabulary/book-rel providers exist but reader UI does not yet emit per-session measurements (speed/taps/replays) — reader emits only completion via P22 recordCompletion; audio cache policy pure only, Piper persistent cache=P28; sentence-tap-replay reader UI NOT added.** NOT device-verified. Next = P28 real Piper audio cache + instant playback.

- Phase 26 (2026-07-18, no ADR, branch feature/phase16-dashboard-redesign): **Curriculum Intelligence + Conversation Continuity.** `lib/language/curriculum_intelligence.dart` (PURE; reasons over EXISTING LanguageKnowledgeGraph + brain — no second graph): `CurriculumIntelligenceEngine{node(id,graph,brain)→CurriculumNode{prerequisites,successors(reverse prereq+buildsOn),connections(touching),difficulty(cefr.index/5·0.7+depth·0.3),estimatedEffort(5+diff·15 min),teachingValue(unlocks·0.15+(1-mastery)·0.5+connections·0.08),mastery}, missingPrerequisite(weakest<0.5), blockingConcept(max unlocks·(1-m) among engaged-weak), almostMastered(0.35-0.8 band), nextToStudy(focus-prereq→focus-if-weak→blocker→almost→frontier via hiddenConnections), journeys(graph,brain)→List<LearningJourney{stages(difficulty-ordered JourneyStage),progress,currentStage,milestone(≥0.5/'complete')}> from brain.connections.clusters — untouched domains = NO journey}`. `lib/language/conversation_continuity.dart` (PURE; conversation memory NOT learner memory): `summarizeConversation(ConversationContext)→ConversationSummary{arc:ConversationArc,topics,openQuestions(last teacher turn w/ ?/¿ unanswered),promises(_promisePattern 'next time|la próxima vez|luego veremos|next we|después'),pendingExercise(context.activeExercise),roleplay(context.roleplay)}`; `buildContinuation(summary)→ConversationContinuation{opener,thread}` priority roleplay→exercise→question→promise→topic→null-null (empty = start fresh, never pretend). `lib/language/error_taxonomy.dart`: `ErrorCategory` ×16; `classifyMisconception` (falseFriend rel→falseFriend, interferesWith/en:-source→englishTransfer, id substrings→verbTense/articleGender/preposition/vocabulary/grammar); `classifyAttempt({previouslyMastered,fastResponse,spoken,speakingConfidence})` (mastered+fast=careless, mastered+slow=memoryLapse, spoken+conf<0.4=confidence); `strategyFor(category)→ErrorTeachingStrategy` distinct per category. `lib/language/teacher_packet.dart`: `TeacherPacket` (ONLY thing generator receives: plan+continuation+state+currentNode+journey+known/unknown+connectionOpportunities+mentalModelInsight+reflection+correctionPolicy+languagePolicy+teachingStyle+objective+summary); `buildTeacherPacket({brain,graph,context,supportMode})` fully derived; `serializeTeacherPacket` deterministic omit-absent; `packetPrompt` merges into P25 buildTeacherPrompt (constraints intact). NO providers/UI this phase — library-level, consumed via packet path; tutor wiring lands w/ real GGUF (P30) to protect verified flows. 334 tests (+14 `test/language_phase26_test.dart` — uses hand-built 10-node graph + real OfflineReasoningEngine brain). analyze clean, build green. NOT device-verified. Next = P27 PDF/EPUB extraction backend.

- Phase 25 (2026-07-18, no ADR, branch feature/phase16-dashboard-redesign): **Local LLM integration (offline).** Principle: TeacherBrain decides WHAT/WHY/WHEN, LLM only words HOW, pipeline speaks. `lib/language/local_llm/`: `llm_prompt_builder.dart` PURE `buildTeacherPrompt({brain,plan:TeacherResponsePlan,context:ConversationContext,userMessage,supportMode,maxCorrections=1})→LlmPrompt{system,user,constraints:LlmConstraints{targetLanguage,nativeLanguage,mentorMode,maxCorrections,doNotRepeat}}` (system encodes intent/stage/pacing/objective/socraticPrompt-or-message/connection-names/one-correction/memory/reflection/mentalModel + facts-only + do-not-repeat from context.usedPhrasings). `llm_memory.dart` `ConversationContext{turns(cap12),topic,roleplay,pendingQuestion,activeExercise,usedPhrasings}`+`ConversationTurn` — CONVERSATION-scoped NOT learner memory; `.withTurn/.markUsed/.copyWith`. `local_llm.dart`: `LocalLlm implements AiChatModel{ready=false,isReady,complete→throws until model loaded}` (seam for real GGUF) + `DeterministicTeacherVoice.word(plan,context,brain)→String` (pure; per-intent Spanish/English variant lists; `_pick` rotates by `(turns.length+intent.index)%len` skipping usedPhrasings = variation-without-randomness, no repeats). `llm_pipeline.dart` `LlmPipeline{voice,intelligence}.respond({brain,context,userMessage,supportMode})→LlmResponse{text,prompt,context}` = plan(turn=turns.length)→prompt→voice→speechSafeText/immersion-drop→advance context. `llm_model_manager.dart` PURE `LlmModelManager{status/ensureDownloaded/delete/verify}` over `LlmModelRepository`+`LlmModelDownloader` seams; `LlmModelStatus{absent,downloading,verifying,ready,failed,deleting,corrupt,versionMismatch}`; consts llmModelVersion/Type(Small)/SizeBytes/ContextLength(4096)/Sha256(PLACEHOLDER)/Url; version mismatch=upgrade. `llm_isolate.dart` msg contract (LlmLoadCmd/GenerateCmd(gen,system,user)/Cancel/ResultMsg/ErrorMsg — NO streaming). `llm_repository.dart` seam+InMemory+`LlmModelInfo{version,sizeBytes,path,sha256,contextLength?,modelType?}`(json). `infrastructure/llm_downloader.dart`: `PrefsLlmModelRepository`+`GgufModelDownloader`(HttpClient+`.part` resume+SHA256 via **crypto ^3.0.0 dep**; PLACEHOLDER sha skips compare). Providers: `llmModelRepositoryProvider/llmModelManagerProvider/localLlmProvider/llmPipelineProvider`. `/llm-settings` screen (download/delete/size/version/type/context/status) from voice_settings. 320 tests (+14 `test/language_local_llm_test.dart`). analyze clean, build green. **HONEST SEAM: real GGUF inference (llama.cpp binding + isolate) NOT done — deterministic voice is shipping generator, words plan offline; tutorModelProvider STILL DemoTutorModel by default (swap to LlmPipeline lands w/ real model to protect verified flows).** NOT device-verified. Next = P26 premium cloud generator (optional).

- Phase 24 (2026-07-18, no ADR, branch feature/phase16-dashboard-redesign): **Teacher Intelligence & Conversation Engine.** `lib/language/teacher_intelligence.dart` (PURE, consumes ONLY TeacherBrain, no UI/persistence/state): `TeacherIntelligenceEngine{stageForTurn(turn,{lessonLength=8})→LessonStage(greeting/warmUp/review/connection/discovery/practice/challenge/reflection arc), conversationState(brain,{turn})→ConversationState{stage,objective,activeConceptId,topic(interests.first),confidence(profile.confidence.overall),energy(motivation.momentum→0-1)}, opportunities(brain)→ranked List<TeachingOpportunity> (recovery pri0 → currentConceptId correct pri1 → connection.suggestion pri2 → mentalModel discover pri3 → curiosity encourage pri4), decide(brain)→TeacherDecision{intent:TeacherIntent(greet/warmUp/review/connect/discover/practice/challenge/encourage/correct/reflect/previewNext),rationale,conceptId?}, pacing(brain)→PacingAction (recoveryMode/strained→recoverConfidence, tooDifficult→slowDown, tooEasy→increaseChallenge, needsRepetition→review, strongReader→tellStory, avoidsSpeaking→practiceSpeaking, else stayTheCourse), memory(brain)→ConversationMemory? (connectionMoments.first "Remember…" else lessonHistory.last else null), correction(brain)→CorrectionPlan? (weakest grammar, praise+one-thing+why anchored to _familyOf domain cluster; null if none), moment(brain,decision)→TeachingMoment (discover=show+socraticPrompt NOT announce; connect=names family; etc), reflection(brain)→ReflectionPlan{improved(Trend.improving skill)/needsWork(weak grammar)/next(secondary obj)/homework(suggestion)}, plan(brain,{turn})→TeacherResponsePlan{state,moment,pacing,correction?,memory?,reflection?(only at reflection stage)}}`. `_familyOf(brain,conceptId)` = same-domain cluster member names. Philosophy encoded: invisible/Socratic/connection-first teaching; deterministic; nothing fabricated (empty/null when brain lacks data). Providers: `teacherIntelligenceProvider`, `teacherPlanProvider`(TeacherResponsePlan? from brain). Tutor `start` now inserts `intelligence.memory(brain)?.reference` between greeting and opener (persistent-teacher feel; additive). **P25 local LLM will consume this — engine decides WHAT/WHY/WHEN, LLM only HOW to word; LLM is NOT the teacher.** 306 tests (+14 `test/language_intelligence_test.dart`). analyze clean, build green. NOT device-verified. Next = P25 Local LLM (AiChatModel impl consuming teacherPlanProvider + ReasoningEngine).

- Phase 23 (2026-07-18, no ADR, branch feature/phase16-dashboard-redesign): **Local Whisper architecture + speaking analytics (offline speech-in).** `lib/language/whisper/`: `whisper_service.dart` — `WhisperService{engineLabel,isReady,transcribe({langCode})→WhisperResult{transcript,confidence?,durationMs?,language?},cancel}` + `NoopWhisperService(scripted)` + `FallbackWhisperService(SpeechService)` (label 'Platform recognizer (fallback)', delegates listen); `whisper_model_manager.dart` — PURE `WhisperModelManager{status(),ensureDownloaded(onState),verify(),delete()}` over `WhisperModelRepository` + injected `ModelDownloader` seam; `WhisperModelState{status:absent/downloading/ready/verifying/error,progress,info,error}`; stale `whisperModelVersion` ⇒ absent (never reuse); `whisper_repository.dart` — repo seam + InMemory; `whisper_isolate.dart` — serializable msg contract (WhisperLoadCmd/TranscribeCmd(gen,samples)/Cancel(gen)/TranscriptMsg/ErrorMsg) mirroring Piper isolate; `whisper_pipeline.dart` — `WhisperPipeline(service).capture({target,langCode,retries,conceptId})→SpeakingSession?` + `feedbackFor(session,brain)`. `infrastructure/whisper_downloader.dart` — `PrefsWhisperModelRepository` + `HttpModelDownloader` (HttpClient stream + `.part` resume + `compute(_extractArchive)` tar.bz2). `lib/language/speaking_session.dart` (pure): `analyzeSpeaking(target,transcript,{durationMs,retries,conceptId})→SpeakingSession{pronunciation(scorePronunciation),fluency(wps/2, null w/o duration),hesitationCount(fillers+adjacent-repeats),fillerCount,repairAttempts,confidence(pron-penalty, null if !completed),completed,conceptId}`; `speakingOutcome(s,day)→LessonOutcome`; `connectionFeedback(s,brain)` names domainAncestor cluster family (≥2) else null (no fabrication). Providers: `whisperModelRepositoryProvider`(Prefs), `whisperModelManagerProvider`(HttpModelDownloader), `whisperServiceProvider`(FallbackWhisperService default — swap point for real sherpa isolate), `whisperPipelineProvider`, `speakingSessionsProvider`(Notifier<List<SpeakingSession>>, resets on lang switch). Speaking `attempt()` now routes through whisperPipeline.capture → appends SpeakingSession; brain `BrainInputs.lessonHistory` = reading outcomes + `speakingOutcome` per session. Screen `whisper_settings_screen.dart` `/whisper-settings` (download/verify/delete/size) linked from voice_settings. 292 tests (+14 `test/language_whisper_test.dart`). analyze clean, Android build green. **HONEST SEAM: real on-device sherpa OfflineRecognizer inference + raw-PCM mic capture NOT done (needs audio-capture plugin + device verify) — staged like Piper scaffold→real; interface/manager/pipeline/analytics complete, only whisperServiceProvider binding moves. Speaking-session persistence across restart also a seam (in-run only via provider).** NOT device-verified. Next = P24 Local LLM (consumes WhisperService + ReasoningEngine interfaces).

- Phase 22 (2026-07-18, no ADR, branch feature/phase16-dashboard-redesign): **Phase15-FINAL MERGED (75a8ea7) + Learning Experience Engine.** Merge: real Piper (sherpa_onnx ^1.13.4 + audioplayers + archive + path_provider; background-isolate inference, device-verified ANR fix c1c3601) + es-novela-faro novel + story chapters now IN this branch; conflicts docs/deps only, code auto-merged (providers hold Piper singletons `platformSpeechProvider`/`piperSpeechProvider` + novella loading alongside brain wiring); speechEngineProvider default piper. `lib/language/experience.dart` (pure): `mineVocabulary(text,curriculum,conceptMastery,{minLength=4,maxWords=20})`→`List<MinedWord{word,count,known(mastery≥0.5),conceptId}>`; `ReadingRecord{day,storyId,title,topics,knownRatio,unknownWords}` (+json); `buildReadingRecord(story,mined,day)` (knownRatio = comprehension proxy); `outcomesFromRecords`→List<LessonOutcome> (**first lessonHistory producer**); `discoverInterests(records)`→weighted Interest list from Story.topics of finished books (empty⇒empty, no fabrication); `importPlainText({id,title,text,level=a2,maxPageChars=420})`→Story (paragraph pages, sentence-split long ones, empty translations); `BookImportParser` seam typed for PDF/EPUB. `lib/infrastructure/prefs_experience_repository.dart`: `ExperienceRepository` seam (records cap 200/books/savedWords) + Prefs impl + InMemory double. Providers: `experienceRepositoryProvider`, `experienceRevisionProvider` (bump-to-reload), `readingRecordsProvider`, `importedBooksProvider` (merged into storiesProvider shelf), `ReadingExperienceController{recordCompletion(story) — mines vocab + persists + revs; importText}` = `readingExperienceProvider`. Reader completion (`_showComplete=true` site) calls recordCompletion → **brain BrainInputs now gets interests+lessonHistory from real records** (two P17-20 empty seams LIVE). Library appbar "Import text" paste dialog (`_showImportDialog` in language_stories_screen). 278 tests (+9 `test/language_experience_test.dart`). GOTCHA: widget tests MUST override `experienceRepositoryProvider` w/ InMemory (brain awaits readingRecordsProvider → real prefs plugin absent under test → brain never resolves → dashboard/tutor tests fail with 0-widget finds). analyze clean, build green. NOT device-verified this phase. Remaining P22 seams: PDF/EPUB parser backends, audio cache/prefetch in Piper (WAVs currently per-request + deleted), parallel-text sentence highlight/tap-replay, saved-words UI, reading speed/replay analytics, recommendations UI. Next = P23 Local Whisper.

- Phase 21 (2026-07-18, no ADR, branch feature/phase16-dashboard-redesign, built ON P20): **Unified Language Pipeline + persistent teacher.** `lib/language/pipeline.dart` (pure): `speechSafeText(reply,target,native)` = strict voice gate (sentence split + `isNativeSentence` via small `_functionWords` sets en/es, native≥2 hits && >target → support) — English NEVER reaches Spanish TTS; `splitTeacherReply`→`TeacherReplyParts{target,support}`; `sanitizeUserInput` (control chars + leading `\|/~^` junk + ws collapse — fixes `\|Si`); `teacherGreeting(brain)` (top curiosity else top notebook observation — real data); `adaptiveFeedback(score,brain)` (references connectionMoments + recoveryMode); `explainWord(word,brain,curriculum)`→`WordExplanation{teachLine(explainByConnection),relatedNames(graph edges),mentalModelInsight,translation(VocabularyConceptNode.translations),conceptId,isEmpty}` — teach first, dictionary second, honest-empty for unknown. Providers: `teacherSupportModeProvider` StateProvider<TeacherSupportMode{immersion,mentor}> default mentor. Tutor screen: `_speak` gates through speechSafeText (native='es'↔'en' from selectedLanguageProvider); session chips + Immersion/Mentor FilterChip; bubbles split — mentor shows italic support under Spanish body, immersion hides; fully-native line (greeting) shown as-is but never spoken. TutorSessionController: `start` prepends teacherGreeting as first tutor turn; `send` sanitizes input + dedupe guard (identical consecutive tutor reply → append "¿Algo más...?"). **DemoTutorModel duplicate-reply ROOT FIX**: `_immersion` es non-opening now rotates 5 distinct reacting questions (`beats[(turns-1)%len]`) + `_reactEs` acks vary by user.length%4 + connection line "Funciona igual que «tener hambre»"; `_beat` clamp→modulo (conversation repeated last beat after 4 turns). **Dynamic speaking**: SpeakingController.start w/ empty focus pulls brain (recentlyActivated + engaged-not-known nodes) + rotates drill order by streakDays — different material each day, deterministic. **Reader producer #1**: `_TappableTargetText` (word Wrap, onLongPress → `_showWordExplanation` bottom sheet: teachLine → mentalModel → related chips → "Dictionary: …" last). 268 tests (+11 `test/language_pipeline_test.dart` incl. immersion-variety regression). analyze clean, Android build green. NOT device-verified. **NOTE: origin/feature/phase15-premium-offline-voice-final (e8e214b real Piper+novella) NOW ON REMOTE — unmerged, merge = separate task (conflicts likely in speech/tutor/reader files).** Next = P22 Learning Experience Engine (PDF/EPUB import, audio cache, lesson outcomes, interest discovery).

- Phase 20 (2026-07-18, no ADR, branch feature/phase16-dashboard-redesign, built ON P19): **Learning Profile + Teaching Style Engine + Adaptive Pedagogy.** `lib/language/learning_profile.dart` (pure): `deriveLearningProfile({facts, learningDna, history, streakDays})` → `LearningProfile{traits:List<LearningProfileTrait{kind:LearningTraitKind, description, evidence}>, confidence:ConfidenceModel{bySkill,overall} (SEPARATE from mastery — baseline from DNA low/highConfidence, speaking avoidance ×0.6), motivation:MotivationModel{state:flowing/steady/strained/unknown, momentum, rationale} (needs ≥2 snapshots), learningSpeed?/difficultyTolerance? (null <10 answers)}`. Trait sources: core DNA strings (benefitsFromRepetition/repeatsMistakes→needsRepetition, fastResponder→fastLearner, slowButAccurate→slowButDurable, strugglesUnderTimePressure, consistent, lowConfidence→respondsToEncouragement) + skill balances (listening-reading>0.15→strongListener, reverse→strongReader, avg(others)-speaking>0.2→avoidsSpeaking). `lib/language/teaching_style.dart` (pure): `TeachingStyleEngine.decide({facts,profile,history})`→`PedagogyDecision{style:TeachingStyle, correctionStyle:minimal/gentle/detailed, difficulty:DifficultyFit(tooEasy≥0.9/ideal/tooDifficult<0.5/unknown<10ans), recoveryMode, rationale}`; recovery = `detectStruggle` (mastery declining across last 3 snapshots) OR tooDifficult → reviewFirst+gentle, beats everything; strained motivation → encouragementFirst+minimal; else profile-driven (strongReader→storyFirst, avoidsSpeaking→conversationFirst, needsRepetition→reviewFirst, fastLearner+tooEasy→challengeFirst, default connectionExplanation). `predictSuccess({conceptId,conceptMastery,prerequisiteIds,accuracy})`→0-1 (0.55·prereqs+0.2·own+0.25·accuracy; <0.4=schedule prereqs first). `computeReadiness`→`ReadinessScores{speaking/reading/conversationReadiness, retention(last/first avg mastery, ≥2 snaps), learningEfficiency}` — null when unmeasured. `TeacherReflection` typed (NO producer yet — lands w/ lesson outcomes P22-roleplay). Brain += `profile/pedagogy/readiness/reflections`; `BrainInputs.history:List<NotebookSnapshot>` (provider passes loaded history); engine reuses one `computeStreak`. `chooseTeachingStrategy` honors `pedagogy.recoveryMode` FIRST (review, no new concepts). All derived/deterministic/offline; premium swaps only ReasoningEngine. 257 tests (+14 `test/language_phase20_test.dart`). analyze clean, Android build green. NOT device-verified. GOTCHA: learning_profile↔teacher_brain circular import (legal Dart; LearnerFacts from teacher_brain, LearningProfile into brain). Next = P21 Language Pipeline + Mentor/Immersion.

- Phase 19 (2026-07-18, no ADR, branch feature/phase16-dashboard-redesign, built ON Connection Engine): **Adaptive Lesson Generator + Curiosity Engine + Mental Model Builder** — brain now proactively teaches. `lib/language/mental_models.dart`: `buildMentalModels({graph,misconceptions,maxModels=3})`→`List<MentalModel{title,insight,anchorConceptId,relatedConceptIds,kind:contrast/family/pattern}>` (curated por/para, ser/estar, tener-not-to-be via `_curatedFor` on cluster members; generic family for clusters≥3; deterministic language-knowledge text, NOT learner data); `discoverPatterns(graph)`→`List<LanguagePattern{type:verbFamily/grammarFamily/semanticGroup/interference/falseFriend/progression}>` from clusters+edge types. `lib/language/curiosity.dart`: `CuriosityNote{text,priority,conceptIds}` + `discoverCuriosities({facts,connections,misconceptions,storiesAvailable,maxNotes=3})` (repeated-misconception occ≥3 pri1, avoiding-speaking level<0.3 pri2, ready-to-read vocab≥0.4+stories pri3, almost-mastered 0.7-0.85 pri4; capped, condition-gated=no spam); `ConnectionMoment` + `buildConnectionMoments(graph)` from strongConnections. `lib/language/lesson_generator.dart`: `AdaptiveLessonGenerator.generate(brain,{stories})`→`LessonPlan{todaysFocus,steps(Known→Connected→New→Practice→Reflection→ConnectionReview→MentalModel),recommendations:List<LessonRecommendation{kind:today/review/challenge/recovery/stretch/conversation/reading/speaking/grammar/vocabulary/story,title,rationale,conceptIds,activityHint}>,mentalModel?}` + `byKind()`. CONSUMES brain (not in it) = orchestrator; existing buildDailyLesson/dailyLessonProvider UNTOUCHED (feeds brain). Brain gained `mentalModels/patterns/curiosities/connectionMoments`; reasoning_engine populates them + BrainInputs `storiesAvailable`; engine folds top mentalModel(pri0)+top curiosity(pri8) into notebook as `ObservationCategory.mentalModel/curiosity` (dashboard icons added: hub/lightbulb/auto_awesome) → dashboard shows teaching w/ ZERO widget change. Providers: `lessonPlanProvider` (Provider<LessonPlan?> from brain+stories). Reading/speaking integration = ARCHITECTURE ONLY (recommendations on brain/plan; `explainByConnection` from P18; UI producers later). All deterministic/offline/single-source (derived from brain, no parallel store). 243 tests (+10 `test/language_phase19_test.dart`: mental models/patterns/no-fabrication, curiosity cap+repeated+moments, lesson-plan arc+recs+byKind, brain integration+notebook surfacing). analyze clean, Android build green. NOT device-verified. Next = P20 Language Pipeline + Mentor/Immersion.

- Phase 18 (2026-07-18, no ADR, branch feature/phase16-dashboard-redesign, built ON Teacher Brain): **Unified Teacher + Connection Engine.** `lib/language/connections.dart` (pure/offline) `buildConnectionGraph({relations, conceptNames, conceptMastery, recentlyActivated, knownThreshold=0.5, maxEdges=60})` → `ConnectionGraph{nodes:Map<String,ConceptNode{mastery,known,recentlyActivated}>, edges:List<ConceptEdge{type:ConnectionRelationType,strength}>, clusters:List<LearningCluster>(by domainAncestor=first-4 `:`-segments), strongConnections(both known), weakConnections(both engaged, shaky), hiddenConnections(one known+other mastery==0=teaching frontier), suggestions:List<ConnectionSuggestion{anchorId,anchorName,relatedIds/Names,rationale}>}`. Relation edges mapped from curriculum `LanguageRelationType`; family edges star-connect engaged concepts sharing a domain to the strongest member. `ConnectionRelationType` enum includes future-producer values (cognate/collocation/idiom/pronunciationPattern/genderPattern/register) NOT emitted yet (no data — architecture only, not faked). `explainByConnection(conceptId,graph)`→String? (reading-tap: explain via known neighbour; producer later). Integrated: `TeacherBrain.connections`; `TeacherObservation.conceptIds` added; buildTeacherNotebook takes `connectionSuggestion` → emits `ObservationCategory.connection` note. **Unified Teacher** `lib/language/teaching_planner.dart` `chooseTeachingStrategy(TeacherBrain)`→`TeachingChoice{mode:TutorMode,focusConceptId?,rationale,connection?}` (priority: currentConceptId repair-via-connection → lagging speaking/conversation<0.4 → strong-anchor buildout → review). `LearnerObjectives.currentConceptId` added. **Removed visible 6-mode grid** from language_tutor_screen — `_ModeSelector` now shows hero + `_TodaysLessonCard` (reads teachingChoiceProvider, shows rationale, "Start today's lesson" → tutorSession.start(mode,focusConceptId)). `_modes`/`_ModeInfo` KEPT (session chip + lesson card use them). Providers: `teachingChoiceProvider`; teacherBrainProvider now passes relations=graph.relations + recentlyActivated(plan+misconception concepts) + currentConceptId. All deterministic/offline. 233 tests (+10 `test/language_connections_test.dart`: graph classification/suggestion/no-fabrication/explainByConnection + strategy selection + connection note). analyze clean, Android build green. NOT device-verified. Future producers/seams: interest discovery, per-phoneme pronunciation edges, reading-tap wiring, lesson-outcome recording (feed connections + brain). Next = P20 conversation/roleplay engine (first producer of lesson outcomes + interests).

- Phase 17-Brain (2026-07-18, no ADR, branch feature/phase16-dashboard-redesign, built ON the notebook engine): **Teacher Brain — central derived source of truth.** `lib/language/teacher_brain.dart` = immutable SSOT aggregate: `TeacherBrain{identity(LearnerIdentity: native/target/name, currentLevel=cefr, longTermGoal, streakDays, estimatedVocabulary), facts(LearnerFacts), notebook(TeacherNotebook), interests, learningDna, objectives(current/secondary/longTerm), lessonHistory}`. FACTS vs OBSERVATIONS split: `LearnerFacts{skills:Map<LanguageSkill,SkillState{level,confidence,trend}>, grammar:List<GrammarPoint{status:mastered/learning/weak/locked,confidence}>, vocabulary(VocabularySummary mastery+estimatedKnown), pronunciation(PronunciationState confidence+trend), accuracy, totalAnswered, cefr}`; enums `Trend{improving/stable/declining/unknown}`, `GrammarStatus`. `computeStreak(days,today)` pure (today counts active + walk back). `lib/language/reasoning_engine.dart` = **`ReasoningEngine` interface (premium swap point)** + `OfflineReasoningEngine` (deterministic; `assemble(BrainInputs)`→facts+grammar-buckets-from-conceptMastery(':grammar:' ids)+per-skill trend vs previous snapshot+streak+estimatedVocab(vocabMastery×vocabularyPoolSize)+calls buildTeacherNotebook). Explainability: `TeacherObservation.evidence:List<Evidence{label,value,delta?}>` added in notebook.dart; buildTeacherNotebook now attaches evidence to grammar/vocab/skill/listening-vs-speaking/pronunciation/trend notes; dashboard `_TeacherNotebookCard` reads `teacherBrainProvider` (was teacherNotebookProvider), header shows working-level + N-day-streak pill, each note tappable → `_showObservationEvidence` bottom sheet ("Based on" + delta pills). Providers: `reasoningEngineProvider`(OfflineReasoningEngine default)+`teacherBrainProvider`(FutureProvider<TeacherBrain?>: builds BrainInputs from live state, saves today's snapshot). Empty-but-typed seams (no data source yet, NOT faked): interests auto-discovery, per-phoneme pronunciation, writing skill, lessonHistory outcomes (LessonOutcome model exists, no producer). 223 tests (+9 `test/language_brain_test.dart`: facts/skill-trend/grammar-buckets/streak/estimatedVocab/evidence). analyze clean, Android build green. NOT device-verified (widget+unit chain instead). GOTCHA: dashboard must NOT import teacher_brain.dart (unused_import — TeacherBrain type flows via provider inference; only notebook.dart symbols named directly). Next = P18 unified adaptive Teacher (reads the brain).

- Phase 17 (2026-07-18, no ADR, branch feature/phase16-dashboard-redesign, built ON phase15): **Teacher's Notebook engine — first real cross-session persistence.** `lib/language/notebook.dart` (pure) `buildTeacherNotebook({mastery, misconceptions, accuracy, totalAnswered, baseLevel, conceptNames, pronunciation/listening/conversation means, previous NotebookSnapshot?, nextConceptName})` → `TeacherNotebook{observations:List<TeacherObservation{text,category,kind,priority}>, cefrEstimate}`. Notes grounded in real data only (omit when a signal is unmeasured); new-learner → single honest starter note. `estimateCefr(baseLevel,avgMastery)` coarse band ("around A1"/"A1+"/next). Persistence seam `lib/language/notebook_repository.dart` (`TeacherNotebookRepository` load/save + pure `mergeSnapshot` one-per-day, cap `notebookHistoryCap`=60; `InMemoryTeacherNotebookRepository`) + disk adapter `lib/infrastructure/prefs_notebook_repository.dart` (shared_preferences, JSON under `teacher_notebook_history_v1`). **Added shared_preferences ^2.3.0** (first on-disk persistence; offline, no account). Providers: `teacherNotebookRepositoryProvider` (Prefs default; Firestore swap point) + `teacherNotebookProvider` (FutureProvider: builds live notebook, trends vs most-recent prior-day snapshot, writes today's snapshot each rebuild — idempotent per day via mergeSnapshot). Dashboard `_TeacherNotebookCard` now consumes `teacherNotebookProvider` (async; category icons, "Working level: around X" header, plan lines highlighted); tappable `_TeacherNotesCard` misconception detail stays below it. Removed P16 placeholder `TeacherNote`/`teacherNotesProvider`. 214 tests (+11: `language_notebook_test.dart` — engine/trend/CEFR/snapshot-JSON/merge-cap/in-memory+prefs round-trip). GOTCHA: tests override `teacherNotebookRepositoryProvider` with InMemory (real prefs plugin would throw under test); Prefs test uses `SharedPreferences.setMockInitialValues({})`. analyze clean, Android debug build green. NOT device-verified (widget test drives full provider chain instead). Next per roadmap = P18 unified adaptive Teacher.

- Phase 16 (2026-07-18, no ADR, branch feature/phase16-dashboard-redesign, built ON phase15): Lab home redesign into teacher-focused dashboard. Removed the large Reading/Speaking/Conversation launch cards (features unchanged, reached via bottom nav Library/Speaking/Tutor). Five sections: Teacher's Notes (expanded, leads), Today's Goals (placeholder daily bars), Progress summary (reuses `_SkillMasteryCard`), Current focus (active lesson + live plan `_LessonPreviewCard`/`_PracticeCta`), Recommended next lesson (first non-repair block). Deleted `_ReadingRecommendation`/`_LaunchTile`. Base branch chosen = phase15 (matches running app); main is 5 phases behind. NOTE: Phase 15-FINAL commit e8e214b (novella+real Piper) NOT on this machine or remote.
- Piper ANR fix (2026-07-18, branch feature/phase15-premium-offline-voice-final): CONFIRMED on-device (CPH2037): synchronous `OfflineTts.generate()` on the UI isolate froze the main thread (~5min) → ANR. FIX in `piper_speech_service.dart` ONLY: long-lived background isolate (`_piperIsolateEntry`) owns OfflineTts + model load + inference; communicates via SendPort/ReceivePort (`_LoadCmd`/`_GenCmd` → events `loaded`/`wav`/`empty`/`error`). Main isolate: download (stream) + extract via `compute(_extractArchive)`, then `_ensureIsolate`+`ensureVoice` (load once, `_engineReady`/`_loading` guards, `LOAD model ok loadCount=1`), `speak` serialized via `_busy` future-chain + `_gen` cancel token, `_synthesize` sends `_GenCmd` awaits matching `wav` (guards `myGen!=_gen`), `_playFile` plays + completes on `onPlayerStateChanged`(completed/stopped/disposed) so `stop()` is instant (no 7s timeout). `stop()`=`_gen++`+`_player.stop()` immediate; `dispose()` kills isolate/ports/player + deletes temp wavs; on failure logs stack + reported fallback to `_platform.speak`. DEVICE-VERIFIED via logcat: isolate×1, load×1, synth-on-isolate, 50× barge-in stress no-crash/no-ANR, UI responsive mid-synth (lang toggle re-rendered), instant barge-in. Deps added: sherpa_onnx ^1.13.4, audioplayers ^6, archive ^4, path_provider. GOTCHA: release build non-debuggable → can't `run-as`/`kill -3`/read `/data/anr` (system 0600); `debugPrint` DOES emit in release logcat; gradle daemon caches pub-cache dir deletes (`gradlew --stop`).

- Phase 15 revision (2026-07-17, same branch, docs-only commit): "floating voice sidebar" = **Gboard hardware-keyboard companion toolbar** (emulator advertises host physical keyboard → Gboard suppresses on-screen QWERTY, floats pill w/ mic/backspace/send/emoji/☰; its ☰ menu = Gboard items incl "Show on-screen keyboard" Alt+K). NOT app UI (grep: zero Overlay/FloatingActionButton/Positioned in lib/presentation; tutor input = [_HoldMic][TextField][Send] lines ~599-622). DEVICE FIX: `adb shell settings put secure show_ime_with_hard_keyboard 1` + `am force-stop com.google.android.inputmethod.latin` → docked QWERTY on focus (screenshot-verified, persists). Remember for ALL future emulator sessions: this setting is now ON; keyboard docks; register-flow coords = docked variant.

- Phase 15 FINAL (2026-07-17, no ADR, branch feature/phase15-premium-offline-voice-final, built ON phase15): REAL Piper + flagship novel. Deps: sherpa_onnx ^1.13.4, audioplayers ^6, archive ^4, path_provider. `piper_speech_service.dart` REWRITTEN real: `_voices` map (es→vits-piper-es_ES-davefx-medium, en→en_US-amy-medium) from github k2-fsa/sherpa-onnx releases tts-models; `ensureVoice` → HttpClient streaming download w/ `progress` ValueNotifier → BZip2Decoder+TarDecoder (archive pkg) extract to appDocs/piper/ → `sherpa.initBindings()` once → `OfflineTts(OfflineTtsConfig(model: OfflineTtsModelConfig(vits: OfflineTtsVitsModelConfig(model/tokens/dataDir espeak-ng-data), numThreads:2)))`; speak: spokenText → sentence chunks → `tts.generate(text,sid:0,speed)` → `sherpa.writeWave` tmp wav → audioplayers `play(DeviceFileSource)` await onPlayerComplete (timeout samples/sr+5s) → 200ms breath; `_gen` token cancels loop in stop()/pause() (barge-in). Status ValueNotifiers (PiperStatus idle/downloading/extracting/loading/ready/error) shown in `_PiperStatusCard` in voice settings. Providers: `platformSpeechProvider`+`piperSpeechProvider` singletons; speechServiceProvider switches (piper default). AndroidManifest +INTERNET. VERIFIED logcat: 63,149,192-byte onnx on device (run-as ls), `[PIPER] gen#N chunkM samples=~39k sr=22050`, chunk gaps==audio duration. NOVEL: assets/stories/es-novela-faro.json "La casa del faro" (7 chapters × 6 pages, ~2700 es words, A2, original); story.dart +`chapterTitles`/`chapterStarts`/`hasChapters`/`chapterOf` + `chapters:[{title,phrases}]` JSON parse (flattened pages); storiesProvider also loads the novel asset for 'es'; reader shows "Capítulo N · title"; library card shows "N chapters". 204 tests (novel parse test). GOTCHAS: gradle daemon VFS caches pub-cache dir deletions → "Plugin directory does not exist" after rm+re-fetch — fix `gradlew --stop`; sherpa generate() blocks main isolate (jank on long paragraphs — future: isolate); keyevent 4 with keyboard hidden exits app (use only when kb visible); Gboard floats/docks between sessions → register-field coords shift. LIMITS: en voice untested; novel ~17-20min pure reading; text-size/sepia/tutor-personality not reached.

- Phase 15 (2026-07-17, no ADR, UX only, branch feature/phase15-premium-offline-voice, built ON phase14b): premium-voice arch + immersive reading. speech.dart: `SpeechEngine.piper` added; `speak()` +`speed=1.0` param (base rate × speed, clamp 0.2-1.0) → rate never hardcoded. `infrastructure/piper_speech_service.dart` = SCAFFOLD (implements seam, engine=piper, `piperReady=false`, delegates ALL to fallback platform service — real Piper needs bundled binary+ONNX model, NOT shipped). language_providers: `speechEngineProvider`(StateProvider, default piper)+`speechSpeedProvider`(1.0); `speechServiceProvider` switches PiperSpeechService(platform)/platform by engine. `voice_settings_screen.dart` `/voice-settings` (engine radio + speed chips 0.8-1.2 + Test voice), tutor appbar tune icon opens it; tutor `_speak`+reader `_playParagraph` pass `speed`. Reader: removed forced quiz — last page → `_showComplete` → `CompletionCard` (reader_companion.dart: Continue/Companion/Vocab/Speaking/optional-Quiz); `_showCompanion` opens `ReadingCompanionSheet` (reader_companion.dart, ConsumerStateful, calls `tutorModelProvider.complete([system w/ paragraph, user])` → reply bubble; quick chips). Reader appbar +companion(forum) icon. Library +Bookmarks shelf (isBookmarked). 203 tests. DEVICE-VERIFIED: completion card (no forced quiz), companion sheet opens+wired (demo reply thin). HONEST: Piper=scaffold not functional; companion answers demo-limited; NO 30-min multi-chapter novels (content deferral); Voice Settings code-done but not screenshot-verified (modal blocked nav). GOTCHA: heredoc with Dart quotes breaks under RTK — put big widgets in a separate Write'd file (public classes).

- Phase 14b (2026-07-17, no ADR, branch feature/phase14b-voice-debug, built ON phase14): voice debug. RUNTIME-TRACED (kDebugMode `[TTS]` debugPrints in PlatformSpeechService.speak/stop): engine=`com.google.android.tts`, voice=`es-es-x-eec-network` (es-ES, after accent fix; was es-us-x-sfb-network), rate 0.42/0.44, pitch 1.06; packages flutter_tts 4.2.0 + speech_to_text 7.0.0. `_pickWarmVoice` now prefers EXACT langCode locale (exact&&warm → exact → warm → first) so es-ES stays Castilian. spokenText: exact string logged before synth (clean — no markdown/URL/emoji/bullet); added `\.(\s*\.)+`→`.` collapse (kills `..` from `\n\n`→`. ` joins). Barge-in VERIFIED via logcat: `speak gen=3` then mic `stop()→gen=4` → clause loop `if(myGen!=_speakGen)return` aborts. HONEST STEP5: Google on-device TTS ≠ neural (ChatGPT/ElevenLabs) quality; premium = bind cloud neural (Google Neural2/WaveNet, Azure, ElevenLabs, OpenAI TTS) behind existing SpeechEngine seam (Phase13) — speechServiceProvider swap + keys, no UI change. 203 tests. Emulator can't capture TTS audio → quality judged by traced engine/voice not recording.

- Phase 14 (2026-07-17, no ADR, branch feature/phase14-bugfixes, built ON phase13): bug fixes + UX completion, all device-verified. BARGE-IN FIX (real bug): `PlatformSpeechService` `_speakGen` int token; `speak()` captures `myGen=++_speakGen`, checks `if(myGen!=_speakGen) return` before each clause; `stop()`/`pause()` do `_speakGen++` → clause loop aborts (previously stop only killed current clause, tutor kept talking). Voice engine = flutter_tts (device Google/Samsung TTS, on-device not neural). spokenText +bare URL/email strip (`\b(?:https?://|www\.)\S+|\b\S+@\S+\.\S+`). `reading_state.dart` (module globals, NOT provider/repo): `readingLastPage` Map + `readingBookmarks` Set + `readingRevision` ValueNotifier<int> (bumped in saveReadingPage/toggleBookmark). Reader: `late _phrase=lastReadingPage(id)`, `PageController(initialPage:_phrase)`, onPageChanged saveReadingPage, bookmark uses store. Library REWRITTEN into shelves: `_Shelf` (horizontal ListView) + `_BookCover` (gradient placeholder cover + level badge + author + `${readingMinutes} min · ${phrases.length} ch` + progress bar); shelves Continue-reading(started && !finished)/Spanish-classics(author!='')/Beginner(a1)/Intermediate(a2,b1); wrapped in `ValueListenableBuilder(readingRevision)` so Continue-reading refreshes (Library is a cached IndexedStack child, wouldn't rebuild otherwise). KEYBOARD: floating pill = Gboard system floating mode, NOT app (app inputs are normal bottom fields) — not app-fixable, documented. 202 tests. Emulator-verified: library shelves, reader resume 2/3, Continue-reading shelf w/ 66% progress, tutor Listening state. Barge-in audio not screenshot-able (code-verified).

- Phase 13 (2026-07-17, no ADR, UX only, branch feature/phase13-conversational-ai, built ON phase12): conversational AI + Kindle reading. speech.dart: `SpeechEngine` enum (demo/androidNeural/iosEnhanced/cloud, `.label`) + `SpeechService.engine` getter (Noop→demo, Platform→androidNeural). Tutor `_SessionState`: `_ConvState` enum {idle,listening,processing,speaking,error}; `_holdStart` (stop→listen; empty→error→idle; else processing+voiceOn+onSend), `_holdEnd` (stop finalizes listen on release), `_tapMic` (barge-in stop when speaking), `_speak` now async sets speaking→idle; `_statusFor` returns pill label+color; `_HoldMic` GestureDetector(onLongPressStart/End+onTap) AnimatedContainer color/icon by state + glow when listening; status pill above input (AnimatedSize). Reader: `_ReaderMode` {both,spanish,english} SegmentedButton (Español/Both/English), gates target `if(mode!=english)` + translation `if(mode!=spanish)` + divider `if(mode==both)`; `_bookmarked` appbar toggle (session-only, no persistence). Dashboard: +Speaking/Conversation `_ExpandableSection`s w/ `_LaunchTile` (FilledButton→homeTabProvider tab; conversation:true starts TutorMode.conversation). story.dart: `Story.author` (JSON `author`) + `readingMinutes` getter (words/130 ceil); `_StoryCard` shows author (italic) + `${readingMinutes} min` chip; authors added to 4 classics. 201 tests. Emulator-verified: reader toggle+bookmark+audio, tutor press-hold→red Listening pill. Honest limits: NO Reading Companion overlay, NO shelf taxonomy/persistent progress/bookmarks, NO independent-scroll panes; STT unusable on emulator (no mic) so full conv round-trip = state-UI+code verified only; tutor personality light; pause best-effort.

- Phase 12 premium UX (2026-07-17, no ADR, UX only, branch feature/phase12-premium-ux-ai-experience, built ON phase11): home/reading/voice. main.dart: light `scheme.copyWith` warm-gray surfaces (0xFFF6F4F1 etc) + `scaffoldBackgroundColor: scheme.surface`. Dashboard: `_HeaderCard` now greeting+pills+"Continue learning" FilledButton (context.push practice); sections wrapped in `_ExpandableSection` (Material+ExpansionTile, initiallyExpanded only for Today's plan) — Skill mastery/Teacher notes/Reading recommendation collapsed; new `_ReadingRecommendation` (top story, tap→/story/:id); removed `_SectionHeader`. GOTCHA: ExpansionTile needs a Material ancestor (not DecoratedBox) or it throws in tests — use `Material(...)` not `Container(decoration:)`. Reader audiobook player: state `_playing`/`_speed`, `_rate=0.44*_speed`, `_playParagraph`(stop→speak rate)/`_stopAudio`/`_cycleSpeed`[0.8,1,1.2,1.5]; player Row (skip_previous/play-pause(IconButton.filled)/stop/skip_next/speed TextButton); onPageChanged stops audio. SpeechService seam +`pause()` (Noop no-op, Platform `_tts.pause()` fallback stop). Tutor `_dictate` calls `speech.stop()` first = barge-in. home_shell nav 'Stories'→'Library'; stories screen title 'Reading Library'. New story es-b1-lazarillo (public domain). GOTCHA: dashboard widget test must tap-expand Skill mastery/Teacher notes before asserting Vocabulary/2×, and `scrollUntilVisible(find.text('2×').first,...)` (multiple 2× → single-match error). 201 tests. Honest limits: audiobook pause best-effort; no full press-hold conversation loop (Listening/Processing/Speaking); tutor personality light; categories = CEFR sections not shelves; Platero omitted (copyright).

- Phase 11 (2026-07-17, no ADR, UX only, branch feature/phase11-experience-overhaul, built ON phase12): experience overhaul. Stories JSON rewritten to multi-sentence narrative paragraphs (each phrase = 3-4 sentences; A1/A2/B1). Reader `language_story_reader_screen.dart` redesigned: `PageController`+`PageView.builder` (swipe + animated page transitions), target text `TextStyle(fontSize:25,height:1.55,w600)` left-aligned, translation demoted to `bodyLarge` muted 0.85 alpha, slim `TweenAnimationBuilder`-animated `LinearProgressIndicator` + "n / N" counter, `Continue`/`Comprehension quiz`/`Finish` buttons via `_pageController.nextPage`. speech.dart spokenText adds bullet-glyph + emoji strip (`[•·▪‣●◦∙]` + unicode class `[\u{1F000}-\u{1FAFF}\u{2600}-\u{27BF}\u{2B00}-\u{2BFF}←-⇿️‍]` with unicode:true — literal arrow-range+VS16+ZWJ chars). platform_speech_service prosody slower (es 0.42/en 0.45) + breaths 320/170. Speaking `_Feedback` `_coachLine(score)` warm bands + count-up score TweenAnimationBuilder + trophy icon. Tutor bubble padding h18/v14, maxWidth 520, vertical AppSpace.sm. GOTCHA: obsolete lesson test asserted `phrases.length>=5` (short phrases) — updated to `>=12 words/phrase` for the narrative redesign. 201 tests. Honest scope: reader/stories fully redesigned; other screens got nudges (Phase-10 atmosphere already there); tutor personality left light (validated flow).

- Phase 12 (2026-07-17, no ADR, branch feature/phase12-stories-voice-usability): classic stories + quiz + voice + Stories usability. `story.dart`: `StoryVocab{word,meaning}` + `StoryQuestion{prompt,options,answerIndex}`; `Story.vocabulary`/`.questions` (optional, parsed from JSON keys `vocabulary`/`questions`[answer]; legacy stories → empty). 3 new stories in `assets/stories/es-for-en.json`: `es-a1-liebre-tortuga`, `es-a2-llorona`, `es-b1-quijote-molinos` (public-domain adaptations) with vocab+questions. `language_story_reader_screen.dart`: `_showVocab` modal bottom sheet (Key words, appbar book icon, only if vocab present); `_showQuiz` bool → after last phrase, `Quiz` button (if questions) → `_Quiz` StatefulWidget (GlassCard per question, `_Option`/`_OptState` correct/wrong colouring, running score, Finish pops). `language_stories_screen.dart`: `_groupedByLevel` emits `Level A1/A2/B1` headers (stories already sorted easiest-first by storiesForLevel), Quiz chip on cards. `speech.dart` spokenText: `[—–]`→`, ` and `\.{3,}|…`→`. ` then tidy passes (`replaceAllMapped` NOT replaceAll for `$1` group — replaceAll inserts literal `$1`). 201 tests (content + speech asserts). NOTE: B1 stories only surface when goal targetLevel≥B1 (storiesForLevel cap) — set via onboarding/goals. Honest caveat: English-in-Spanish TTS robotic = platform single-utterance limitation, not fixable without cloud neural per-word lang switching.

- Phase 10 (2026-07-17, no ADR, UI only): onboarding + Hume atmosphere. `onboardingSeenProvider` (StateProvider<bool> in providers.dart) gates `/onboarding`; router redirect: signedIn && !seen → /onboarding, seen && on /onboarding → /. `language_onboarding_screen.dart` = 3-page PageView (welcome/language/goals) writing selectedLanguageProvider + learnerGoalsProvider, then `onboardingSeenProvider=true` + `context.go('/')`. ui.dart adds: `AtmosphericBackground` (Stack: vertical gradient + two `_Glow` RadialGradient circles), `GlassCard` (ClipRRect→BackdropFilter(ImageFilter.blur)→translucent surface + hairline border + InkWell). router.dart `_fadeThrough` = CustomTransitionPage(fade + 0.98→1 scale) applied to practice/concept/story/settings/content/goals via pageBuilder. Applied AtmosphericBackground (transparent Scaffold+AppBar) to dashboard/tutor/stories/speaking/story-reader; GlassCard on _LessonPreviewCard + _SkillMasteryCard + onboarding tiles/cards. 199 tests (onboarding widget smoke test). GOTCHA: AtmosphericBackground is a Stack — wrap the existing body Center/child and add ONE extra `)` closer; keep Scaffold+AppBar backgroundColor transparent so the backdrop shows. GlassCard blur needs something behind (the atmospheric bg) to frost.

- Phase 9 (2026-07-17, no ADR, UI only): shared kit `lib/presentation/ui.dart` — `AppSpace`(xs4/sm8/md12/lg16/xl24/xxl32), `AppRadius`(card20/input16/pill999), `AppMotion`(enter420/easeOutCubic); `GradientHero({child,colors,onTap,padding})` = DecoratedBox(shadow+radius)→ClipRRect→Material→InkWell→Ink(gradient)+Stack(white sheen) [clip gotcha: shadow on OUTER box, clip INNER]; `GlassPill`; `FadeInUp({delayMs,child})` = TweenAnimationBuilder Opacity+translate, stagger via `Interval(delayMs/total,1)` (no timers, settles in test pump budget). Applied: dashboard (sections in FadeInUp+Column(stretch), _HeaderCard/_TutorHeroCard→GradientHero, removed local _GlassPill), tutor (mode hero→GradientHero, grid+bubbles FadeInUp), stories (gradient tile+FadeInUp), speaking intro (gradient halo), story reader (FadeInUp key:ValueKey(_phrase)). Theme main.dart: cardTheme elevation1 + shadowColor .28 + surfaceTintColor transparent + radius; input radius AppRadius.input. 198 tests unchanged, core zero-diff. GOTCHA: AVD is `Medium_Phone` (memory's `flutter_emulator` STALE); emulator crash-prone — cold-boot via run_in_background tool (inline `&` loses HOME env), avoid back-keyevent on login (pops app to home), keyboard-open shifts login layout.

- Phase 8 polish (2026-07-17, no ADR): `spokenText(md)` in `speech.dart` (pure) strips markdown emphasis/code/heading/list markers + link URLs, keeps `¿¡?!` for prosody; `PlatformSpeechService.speak` calls it so TTS never voices `*`. Tutor bubble `_markdownSpans` renders `**bold**`/`*italic*` as styled spans (no literal asterisks); `Text.rich` + soft boxShadow. Tutor reply + practice `TextField`s: `contextMenuBuilder: (_,_) => SizedBox.shrink()` (kills Flutter floating selection toolbar), removed hardcoded OutlineInputBorder (theme filled pill), tutor onSend unfocuses + trims-empty guard. main.dart: refined `textTheme` (Typography.material2021, tighter headings, body height 1.4). 198 tests (3 spokenText). GOTCHA: Dart RegExp has no inline `(?m)` — use `multiLine: true`; leading-marker strip uses `[ \t]` not `\s` so blank-line newlines survive for the sentence-break pause. NOTE: the on-device Gboard floating-keyboard toolbar (icon pill) is a system keyboard mode, NOT app-removable; only Flutter's selection toolbar is.

- Phase 8 — Production Readiness demo slice (2026-07-17, ADR-0026): `lib/language/content_merge.dart` — `mergeApprovedContent(base, approved)` attaches approved unmapped vocab→`VocabularyConceptNode`, phrase/idiom→`PhraseNode` under synthesized domain `<lang>:<level>:vocabulary:ingested` (id e.g. `es:a1:vocabulary:ingested:restaurante`); skips mapped/rejected; base never mutated; `_slug` folds to `[a-z0-9áéíóúüñ-]`. `storyFromApproved(approved, languageCode, level)` → Story id `<lang>-ingested-story` "From your content" (null if <2 sentences). Providers: `ApprovedContentController extends Notifier<List<ContentCandidate>>` + `approvedContentProvider` (watches `selectedLanguageProvider` to reset on switch; add/remove by id); `curriculumProvider` returns `mergeApprovedContent(base, ref.watch(approvedContentProvider))`; `storiesProvider` inserts `storyFromApproved` at index 0 + caps at `learnerGoalsProvider.targetLevel`; `ContentStudioController.approve/reject` add/remove the candidate. Goals: `LearnerGoals{minutesPerDay=25, targetLevel=a2}` + `LearnerGoalsController` (setMinutes clamp 5..60, setTargetLevel) + `learnerGoalsProvider`; `availableMinutesProvider` = goal minutes; tutor goal = "Reach {TARGETLEVEL} {lang}". Screen `language_goals_screen.dart` (`/goals`, minutes Slider 5..60 div 11 + level ChoiceChips a1/a2/b1/b2 + live "aiming for X" card); Lab app-bar `Icons.track_changes_outlined` entry. `PRODUCTION_CHECKLIST.md` new. 195 tests (language_merge_goals_test.dart new; +1 goals widget test). GOTCHA: approve-flow tests override curriculumProvider to `mergeApprovedContent(base, ref.watch(approvedContentProvider))` and must `await curriculumProvider.future` before `studio.ingest`.

- Phase 7 — Content Ingestion (2026-07-17, ADR-0025): `lib/language/ingestion.dart` `ingestLanguageText(text, graph, languageCode)` → `IngestionResult{difficulty, topics, candidates:List<ContentCandidate>}` across `ContentKind` {vocabulary/phrase/sentence/idiom/culturalNote}; maps to concept ids via graph vocab lemmas + phrase texts (folded); idioms matched by phrase OR key noun (conjugated forms). `ContentReviewLog`(approve/reject/pending) + `ContentReviewRepository` seam (InMemory). Providers: `contentReviewRepositoryProvider`, `contentStudioProvider` (ContentStudioController: ingest/approve/reject/clear). Screen `language_content_screen.dart` (`/content`, watches curriculumProvider to warm it; "Use sample" button); Lab app-bar admin entry gated by `authStateProvider.value.isAdmin` (needs `import '../providers.dart'`). Practice `_submit`/`_next` call `FocusManager.instance.primaryFocus?.unfocus()` (keyboard cleanup). 4 new stories (2×A1, 2×A2 — A2 gated by recommendedLevel). 186 tests (language_ingestion_test.dart new). GOTCHA: content-screen widget test needs curriculum future warmed (`ref.watch(curriculumProvider)` in build + `_settle` before tapping sample).

- Phase 6 — Speech & Pronunciation (2026-07-17, ADR-0024): `speaking.dart` `scorePronunciationDetailed` → `PronunciationResult{score, List<PronWord>}` (phoneme-folded Levenshtein per-word align; `scorePronunciation` = façade `.score`). `exercises.dart`: `ExerciseType.listening` items (`ExerciseItem.audio` hidden lemma, options=lemmas) + type-priority sort tiebreak (mc before listening). `signals.dart`: `afterListening` (listeningRecognition EWMA). `recordAnswer(listening:)` also bumps listeningRecognition on lineage. `lesson.dart`: mean pronunciationConfidence/conversationAbility computed from signals → speaking/conversation block weights scale up when low. SpeakingState +`words:List<PronWord>`; speaking screen shows per-word chips. Practice screen renders listening (auto-play + Play again). `platform_speech_service.dart`: `_prosody` per-lang, `_clauses` split (commas too), question rise/slow + exclaim lift. main.dart theme: navigationBarTheme (pill indicator, onlyShowSelected labels), filledButton radius 14, filled inputs. 176 tests (language_speech_test.dart new). Emulator note: no real mic/recognizer → per-word chips + listening STT verified by unit tests, not on-device.

- Phase 5 — Conversation Engine (2026-07-17, ADR-0023): `lib/language/conversation.dart` (`pickScenarioConceptId` weak-weighted, `conversationTurnQuality`). `TutorContext` +scenarioConceptId/scenarioName/scenario/targetVocab; `buildTutorContext(scenarioConceptId:)` pulls ConversationNode scenario + `_targetVocab` from weak concepts. `tutorSystemPrompt` emits `Scenario:`/`Target vocabulary to weave in:`; conversation+immersion dialogue plans rewritten (react/recast/weave/progress/follow-up). `DemoTutorModel` conversation+immersion rewritten multi-turn contextual (reacts to last user msg, recasts soy-cansado→tengo sueño, scene beats). Signal: `conversationAbility` via `afterConversationTurn`; controller `recordConversationTurn` (signal-only, scenario lineage). Session `start` picks weak-weighted scenario for conv/immersion + scores each learner turn. Dashboard Conversation block starts a Conversation session. Voice: PlatformSpeechService speaks sentence-by-sentence with 220ms breaths + question-pitch lift + volume 1.0. 165 tests (language_conversation_test.dart new).

- Phase 4 — Daily Lesson Engine (2026-07-17, ADR-0022): `lib/language/lesson.dart` `buildDailyLesson` (replaces `previewDailyLesson`) — weighted time-budgeted blocks (repair/review/grammar/vocab/pronunciation/story/conversation), `LessonBlock` now carries `reason` + `activity` (LessonActivity: practice/speaking/story/tutor) + `storyId`; DNA traits shape weights; pure (no core import — provider `dailyLessonProvider` computes `dueConceptIds` from core ConceptStats.isDue/lapses, feeds signals/traits/stories/accuracy/`availableMinutesProvider`). Dashboard `_LessonPreviewCard` shows reasons + `_launchBlock` dispatches by activity; old `_StoryRecommendation` removed. Enriched story seeds (narrative, ≥6-word phrases). SpeechService.speak gains rate/pitch; PlatformSpeechService picks enhanced/neural voice + warm defaults (rate 0.44, pitch 1.05). 155 tests (language_lesson_test.dart new). Old lesson tests migrated to buildDailyLesson.

- Content & Voice (2026-07-16, ADR-0020/0021): Stories (`lib/language/story.dart`, `assets/stories/`), Speaking (`lib/language/speaking.dart` — `scorePronunciation` accent-folded token overlap), Speech seam (`lib/language/speech.dart` `SpeechService`; real adapter `infrastructure/platform_speech_service.dart` over flutter_tts+speech_to_text; `NoopSpeechService(scriptedTranscript:)` for tests). Providers: `storiesProvider`, `speechServiceProvider`, `speakingProvider`, `languageBcp47Provider`; `LanguageLearnerController.recordPronunciation`; `LanguageConceptSignals.afterPronunciation`. UI = bottom-nav `HomeShell` (Lab/Stories/Speaking/Tutor, `homeTabProvider`), FAB removed; tutor voice (speak bubbles + "Voice replies" auto-speak + mic dictation). Android `RECORD_AUDIO` added. 147 tests. Gotcha: widget tests over rootBundle-loaded stories/speaking are flaky under the shared binding — cover that logic in `language_content_test.dart` (unit/controller), verify screens on emulator. `_app` in language_screens_test overrides `speechServiceProvider` with NoopSpeechService.

- Phase 3 dialogue depth (2026-07-16, ADR-0018 addendum): tutor prompts carry `MODE:` tag + per-mode `Session flow:` dialogue plans; `validateTutorReply` adds immersion purity gate (≥2 distinct native stopwords → reject; `_stopwords` map in tutor.dart); `LanguageLearnerState.traits` = live Learning DNA (engine.learningDna after every answer) fed to TutorContext; DemoTutorModel = six mode-true strategies, multi-turn aware via assistant-turn count. UI: typing indicator, avatar bubbles, tappable plan blocks, exercise-type interleaving in generateExercises. 137 tests green. Android emulator verified (AVD `flutter_emulator`; pin `adb -s emulator-5554` — ghost offline 5556 may appear; screencap workflow documented in memory).

- Rebrand complete (2026-07-16, ADR-0019): Language Lab = home (`/`), exam routes retired (screens unrouted dead code until package-rename sweep — TASKS Active), multi-language selector (`availableLanguages` + `selectedLanguageProvider`; per-language `_seedScripts`; `_init` RESETS misconception/signal stores before seeding — shared in-memory repos otherwise leak the previous language's state across switches, regression-tested), teal immersion theme, tutor hero + FAB, tutor goals = "Reach A2 {languageName}". 132 tests green.

- Phase 3 foundation complete (2026-07-16, ADR-0018): `lib/language/tutor.dart` — `TutorContext` (skill mastery, weak concepts, misconceptions, signals, goals, DNA traits, focus graph slice; default focus = top misconception), `tutorSystemPrompt` (6 mode personas + `[LEARNER CONTEXT]` block), `validateTutorReply` (structure + grounding; rejected → safe fallback), `LanguageTutor` over `AiChatModel`. `lib/infrastructure/demo_tutor_model.dart` = deterministic vendor stand-in reading the same prompts. Providers: `tutorModelProvider` (vendor swap point), `tutorSessionProvider`. UI: `/language/tutor` + dashboard CTA. 131 tests green (`language_tutor_test.dart` new). Remaining Phase 3: dialogue depth per mode, immersion purity validation, tutor history persistence, vendor adapters (need API keys).
- Phase 2 COMPLETE (2026-07-16, ADR-0017 finish): `lib/language/exercises.dart` — exercises derived from curriculum data (5 text-first types, deterministic via seeded shuffles, `focusConceptIds` first, `checkAnswer` keeps diacritics); `/language/practice` (`languagePracticeProvider`, screen with inline teacher notes + score summary); dashboard CTA. `recordAnswer` now: returns detected misconceptions, awaits `_initFuture`, detects over `lineageConceptIds` (leaf-first) so child-exercise errors implicate ancestor concepts, registers transfer signals on attributed ancestors. 120 tests green (`language_exercises_test.dart` new).
- Phase 2 core (2026-07-16, ADR-0016): `lib/language/misconceptions.dart` (MisconceptionDetector — fires only on wrong answers with graph-authorized interference; MisconceptionLog merges by `conceptId|source`), `signals.dart` extended (`afterAnswer` EWMA alpha 0.3, `LanguageSignalsStore`, repository seams), `lesson.dart` (repair-first preview, Phase 4 stopgap), `lib/infrastructure/language_repositories.dart` (in-memory), `lib/presentation/language_providers.dart` (`LanguageLearnerController` — reuses unchanged core `LearnerEngine` via `toCoreGraph()`, deterministic demo seed), screens `language_dashboard_screen.dart` (`/language`) + `language_concept_screen.dart` (`/language/concept/:id`, live simulate buttons). 110 tests green (`language_tracking_test.dart`, `language_screens_test.dart` new).
- Phase 1 (ADR-0015): `lib/language/` — `entities.dart` (11-tier hierarchy, typed nodes, CEFR, 10 skills), `relationships.dart` (6 relation types, `toCoreGraph()`), `curriculum.dart` + `assets/curriculum/` seeds (es-for-en, en-for-es). Schema drafts: `docs/database/05-language-schema.md`. Core untouched since fork (verify: empty `git diff 3b597b2..HEAD -- app/flutter/lib/adaptive app/flutter/lib/ai`).
- Inherited codebase state: Flutter 3.44.5 (SDK at `C:\Users\Admin\flutter`), Riverpod 3 (`StateProvider` needs `flutter_riverpod/legacy.dart`; `AsyncValue.valueOrNull` is now `.value`), go_router 16. Swap point = providers in `lib/presentation/providers.dart`. Package still named `adaptive_exam_platform` (rename queued for when exercise flows retire exam screens). App ids are already distinct (2026-07-16): Android `applicationId` = `com.adaptiveexam.adaptive_language_platform`, iOS bundle id = `com.adaptiveexam.adaptiveLanguagePlatform` — installs coexist with the exam app; Kotlin namespace/MainActivity package intentionally unchanged until the rename sweep. CI: `.github/workflows/ci.yml`.
- Next: Phase 8 infra — Firestore repository swap behind the existing seams, real speech/AI/analytics vendor adapters, iOS/web parity, package rename (blocked on Firebase keys). See TASKS.md + PRODUCTION_CHECKLIST.md.

## Key Conventions

- Documentation lives in `docs/` split by domain; root-level files are indexes and state.
- Decision records: `docs/decisions/NNNN-title.md`. ADRs 0001–0013 inherited from exam platform (still binding for the core); 0014+ are language-platform decisions.
